// Compatibility entry point retained for the existing server router registry.
// The complete implementation lives in funding-command-router.ts.
export { fundingRouter } from "./funding-command-router";
