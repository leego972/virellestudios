name: Update Free360 Lockfile

on:
  push:
    branches:
      - lamalo-free-360-pipeline
    paths:
      - package.json
      - .github/workflows/update-free360-lock.yml

permissions:
  contents: write

jobs:
  update-lock:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          ref: lamalo-free-360-pipeline
          fetch-depth: 0
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - name: Regenerate lockfile
        run: pnpm install --lockfile-only --no-frozen-lockfile
      - name: Verify patched dependency graph
        run: pnpm audit --audit-level moderate
      - name: Commit lockfile
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add pnpm-lock.yaml
          git commit -m "Update lockfile for patched brace expansion"
          git push origin HEAD:lamalo-free-360-pipeline
